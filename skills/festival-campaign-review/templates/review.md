# <年份> <節慶> 檔期覆盤報告

> 使用方式：所有 `<填入>` 都要換掉。填不出來的欄位寫「無資料」並在該區塊註明影響，
> 不要留空、不要用估的頂替。任何數字都必須能指回資料來源。

---

## 1. 檔期基本資料

| 項目 | 內容 |
|---|---|
| 節慶／檔期 | `<填入>` |
| 節慶當日 | `<填入，由 backplan.py 取得>` |
| 檔期起訖 | `<填入>` |
| 前置節奏 | `<long / mid / short，由 backplan.py 取得>` |
| 品類 | `<填入，對應 category-profiles.md 標籤>` |
| 主推品項／方案 | `<填入>` |
| 主要通路 | `<填入>` |
| 投入預算 | `<填入，或註明無法取得>` |
| 覆盤日期 | `<填入>` |
| 與會者／填寫人 | `<填入>` |

不合格：節慶日期或檔期起訖是憑印象寫的，而不是 `backplan.py` 的輸出。

---

## 2. 一頁摘要

只寫三句話，主管只看這一段也要看得懂。

- **發生了什麼**：`<填入，一句話，含最關鍵的達成率或落差>`
- **為什麼**：`<填入，一句話，寫真正的主因，不是列一堆因素>`
- **下次改什麼**：`<填入，一句話，要能對應到第 7 區的改善項目>`

不合格：三句話都是形容詞（「整體表現不錯」「團隊很努力」），沒有任何具體事實；
或「為什麼」列了五個原因，等於沒有結論。

---

## 3. 數字層：目標 vs 實際

| 指標 | 目標 | 實際 | 達成率 | 資料來源 |
|---|---|---|---|---|
| 營收 | `<填入>` | `<填入>` | `<填入>` | `<填入，如 POS 匯出 / 平台後台 / ERP>` |
| 毛利（額與率） | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| 訂單數 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| AOV | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| 新客數／新客佔比 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| 退貨／取消率 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| 售罄率／節後庫存 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| ROAS 或行銷費用佔比 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| `<品類專屬 KPI>` | `<填入>` | `<填入>` | `<填入>` | `<填入>` |

指標依品類調整，見 `${CLAUDE_PLUGIN_ROOT}/shared/category-profiles.md` 的「常看 KPI」。有原始銷售資料時，
先用 festival-sales-analysis 產出這張表，不要手抄。

**資料缺口**：`<填入哪些指標拿不到、因此哪些結論不成立>`

不合格：只放營收一列就宣告成功；有數字卻沒有資料來源欄；
把折扣、贈品、平台補貼的成本排除在毛利之外，讓達成率好看。

---

## 4. 執行層：里程碑計畫 vs 實際

計畫日欄位一律填 `backplan.py` 輸出的日期，不要自行回推。

| 節點 | 階段 | 計畫日 | 實際完成日 | 落差天數 | 卡在哪／為什麼 |
|---|---|---|---|---|---|
| D-60 | 定策略 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| D-45 | 定機制 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| D-30 | 備彈藥 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| D-21 | 點火 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| D-14 | 黃金期 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| D-5 | 最後衝刺 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| D-day | 節慶當日 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| D+3 | 長尾 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |

節點依該檔期實際跑的前置節奏增減。常見卡點分類：物料交期、通路回覆慢、素材重做、
人力不足、簽核卡關、系統／金流沒到位。

**最關鍵的一次落後**：`<填入是哪一個節點，以及它連帶推遲了什麼>`

不合格：「大致上都有跟上」這種沒有日期的描述；落差原因寫「時間不夠」，
沒有指出到底是誰的哪一段作業超時。

---

## 5. 決策層：假設驗證

把計畫書裡標記 `[假設]` 的每一條抓出來，逐條判定。這區是整份報告最值錢的部分。

| # | 原假設 | 實際發生 | 判定 | 下次怎麼改 |
|---|---|---|---|---|
| 1 | `<填入計畫書原文>` | `<填入，附證據或資料來源>` | `<成立 / 推翻 / 資料不足無法判定>` | `<填入具體做法，不是「再觀察」>` |
| 2 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| 3 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |

**判定為「資料不足」的假設**：`<填入，並寫下次要先埋什麼追蹤才判得出來>`

不合格：整表全部「成立」——代表沒有認真檢驗，或當初根本沒寫下假設；
判定為推翻卻在「下次怎麼改」寫「需再討論」。

---

## 6. 外部因素：誠實區分功勞與順風

| 外部因素 | 方向 | 對結果的影響 | 判斷依據 |
|---|---|---|---|
| `<如平台大促、競品缺貨、天氣、政策、匯率>` | `<順風 / 逆風>` | `<填入，質化描述即可>` | `<填入>` |

- **拿掉外部順風後，還站得住的成果**：`<填入>`
- **可複製 vs 不可複製**：`<填入，哪些是方法、哪些只是這次運氣好>`

不合格：這一區留白或只寫「無特殊外部因素」；把平台級的流量紅利算成自家操作的功勞，
於是下次照抄同一套卻打不出來。

---

## 7. 改善項目

每一項都要有人、有時間點、有驗收標準，否則不要寫進來。

| # | 改善項目 | 負責人 | 下次檔期的哪個節點做 | 驗收標準（做到什麼算完成） |
|---|---|---|---|---|
| 1 | `<填入>` | `<填入姓名，不寫「行銷部」>` | `<如 D-45 定機制前>` | `<填入可判定的產出或狀態>` |
| 2 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| 3 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |

**追蹤機制**：`<填入下次何時、由誰回頭檢查這張表>`

不合格：負責人寫成部門或「大家」；節點寫「下次早一點」；
驗收標準寫「改善流程」這種無法判定完成與否的句子。

---

## 8. 學習紀錄摘要

以下內容可直接複製成 `data/learnings/<年份>-<節慶>.md`，
下次跑 festival-campaign-ideas 或 festival-campaign-plan 前會先被讀取。
只留三到五條，寫給「明年還沒進入狀況的自己」看。

```markdown
# <年份> <節慶> 學習紀錄

- 有效，下次照做：`<填入，含成立的條件>`
- 無效，下次別做：`<填入，含失敗的原因>`
- 被推翻的假設：`<填入原假設 → 實際情況>`
- 時程上的教訓：`<填入哪個節點必須更早，以及不早會發生什麼>`
- 這次沒量到、下次要先埋的追蹤：`<填入>`

資料完整度：`<填入哪幾層有資料、哪幾層是空的>`
```

不合格：條目寫成「執行順利，維持現狀」；寫了十幾條變成流水帳，
下次沒人會讀完；沒有標註資料完整度，讓明年的人誤以為這些結論都有數據支撐。
