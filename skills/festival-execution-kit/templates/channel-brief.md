# 通路／平台／代理商檔期 brief

單頁為限。所有日期取自 `backplan.py` 輸出，不要自行推算。沒有把握的數字留 `<填入>` 或標 `[假設]`，不要編。

- 品牌／單位：`<填入>`
- 通路／平台／代理商：`<填入>`
- 檔期名稱：`<填入>`
- brief 版本與日期：`v<填入>`／`<填入>`

## 1. 檔期與日期

- 節慶當日（D-day）：`<backplan 輸出>`
- 活動期間：`<起>` ~ `<迄>`
- 關鍵節點：提案截止 `<填入>`／素材截稿 `<填入>`／上架 `<填入>`／首批到倉 `<填入>`／下架與退貨 `<填入>`
- 遇週六日的內部作業已提前至前一工作日：是／否

> 沒寫清楚會出什麼包：雙方各自記一組日期，通路以為還沒開跑、品牌以為已經上架，資源位開了卻沒有內容。

## 2. 目標與成功定義

- 這次要通路幫忙達成的一件事：`<填入，例如新客試用、指定品項鋪貨、資源位曝光>`
- 成功／失敗的判斷指標與門檻：`<填入，由雙方歷史資料或合約承諾決定，不套用外部平均值>`
- 量化目標數字來源：`<使用者歷史資料／標明出處的外部報告>`

> 沒寫清楚會出什麼包：檔後各講各話，通路說流量給足、品牌說賣不動，沒有共同基準可以復盤。

## 3. 主推品項與價格機制

| 品項 | 品號／SKU | 建議售價 | 檔期價 | 機制 | 成本負擔方 |
|---|---|---|---|---|---|
| `<填入>` | `<填入>` | `<填入>` | `<填入>` | `<填入>` | `<品牌／通路／各半>` |

- 機制能否與平台券、會員折扣、信用卡優惠疊加：`<可／不可，逐項列出>`
- 折扣底線（低於此不再讓）：`<填入>`

> 沒寫清楚會出什麼包：優惠疊加後單品毛利變負數，或通路擅自加碼折扣，品牌被迫吸收價差。

## 4. 素材規格與截稿日

| 素材 | 尺寸／格式 | 檔案大小上限 | 文案字數 | 交付方式 | 截稿日 | 負責人 |
|---|---|---|---|---|---|---|
| 主視覺 | `<填入>` | `<填入>` | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| 資源位 banner | `<填入>` | `<填入>` | `<填入>` | `<填入>` | `<填入>` | `<填入>` |

- 需要幾輪改稿與各輪回覆時限：`<填入>`
- 品牌使用規範（logo 留白、色值、禁用字）：`<填入或附連結>`

> 沒寫清楚會出什麼包：交件被退，重做趕不上上架截止，資源位錯過就整檔沒有了。

## 5. 上架與陳列需求

- 線上：品項上架位置、活動頁區塊、搜尋關鍵字、標籤：`<填入>`
- 線下：陳列位（堆頭／端架／貨架）、面數、POP 物料與到店日：`<填入>`
- 試吃／體驗人力：由誰出、時段、話術由誰提供：`<填入>`

> 沒寫清楚會出什麼包：貨到了沒人陳列，或 POP 物料到店比商品早兩週，被當垃圾丟掉。

## 6. 庫存與補貨機制

- 首批備貨量與依據：`<填入，依自家歷史出貨與通路預估，不用外部推估值>`
- 安全庫存水位與補貨觸發條件：`<填入>`
- 補貨前置期（下單到到倉）：`<填入>`
- 缺貨時的處理：`<下架／換替代品項／改預購，指定決定人>`
- 節後剩貨處理與退貨條件：`<填入>`

> 沒寫清楚會出什麼包：黃金期斷貨，廣告還在跑、流量進來看到缺貨；或節後剩貨誰承擔沒有共識。

## 7. 雙方負責窗口

| 角色 | 姓名 | 職稱 | 聯絡方式 | 可決定什麼 |
|---|---|---|---|---|
| 品牌主窗口 | `<填入 或 [待指派]>` | `<填入>` | `<填入>` | `<填入>` |
| 通路主窗口 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| 假日／夜間緊急聯絡 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |

> 沒寫清楚會出什麼包：D-day 出狀況時在群組喊人沒人回，或找到的人無權決定要不要下架。

## 8. 驗收方式

- 上架驗收：由 `<誰>` 在 `<日期>` 逐項確認 `<頁面／陳列／價格／優惠碼>`，以 `<截圖／現場照片>` 為證。
- 成效資料：由 `<誰>` 於 `<日期>` 提供 `<欄位>`，格式 `<填入>`。
- 費用結算依據與付款條件：`<填入>`

> 沒寫清楚會出什麼包：檔期跑完拿不到分通路資料，覆盤只能憑感覺，下一次還是重犯同樣的錯。

## 9. 風險與應變

| 風險 | 觸發訊號 | 應變動作 | 決定人 | 觸發期限 |
|---|---|---|---|---|
| 素材延遲 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| 缺貨 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| 價格錯誤上架 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |
| 負面聲量 | `<填入>` | `<填入>` | `<填入>` | `<填入>` |

> ⚠️ 沒寫清楚會出什麼包：出事時第一個小時全花在確認誰能拍板，錯過的是最貴的那段流量。
